# Day 09 练习题

## 练习 1：Fixture + 参数化

```python
# 1.1 测试 Pydantic 模型
# 用 fixture 提供测试数据
# 用参数化测试多种边界情况

class User(BaseModel):
    name: str = Field(min_length=1)
    age: int = Field(ge=0, le=150)
    email: str

# 写测试覆盖：正常情况、边界值、异常情况
```

## 练习 2：Mock

```python
# 2.1 Mock OpenAI API 调用
# 测试一个调用 LLM 的函数，不实际调用 API

async def ask_llm(question: str) -> str:
    client = AsyncOpenAI()
    resp = await client.chat.completions.create(
        model="gpt-4o",
        messages=[{"role": "user", "content": question}],
    )
    return resp.choices[0].message.content

# 写测试，mock 掉 AsyncOpenAI
```

## 练习 3：Agent 测试

```python
# 3.1 测试一个完整的 Agent 工具调用链
# Mock LLM 返回工具调用 → 验证工具被执行 → 验证最终结果
```
