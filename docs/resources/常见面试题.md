# Python Agent 开发常见面试题

## 一、Python 基础

### 1.1 list 和 tuple 的区别？
- list 可变，tuple 不可变
- tuple 可以作为 dict 的 key，list 不行
- tuple 占用内存更小

### 1.2 dict 的底层实现？
- 哈希表（hash table）
- Python 3.7+ 保证插入顺序
- 查找/插入/删除平均 O(1)

### 1.3 GIL 是什么？对 Agent 开发有什么影响？
- Global Interpreter Lock，全局解释器锁
- 同一时刻只有一个线程执行 Python 字节节码
- I/O 密集型任务（如调用 LLM API）不受影响，因为 await 会释放 GIL
- CPU 密集型任务需要用 multiprocessing

### 1.4 装饰器的本质是什么？
- 一个接收函数、返回新函数的高阶函数
- `@decorator` 是 `func = decorator(func)` 的语法糖
- 带参数的装饰器是三层函数嵌套

### 1.5 生成器和列表的区别？
- 生成器惰性求值，列表立即求值
- 生成器只能遍历一次，列表可重复遍历
- 生成器省内存，适合大数据集

## 二、Pydantic

### 2.1 Pydantic v1 和 v2 的区别？
- v2 用 Rust 核心，性能提升 5-50x
- API 变化：`.dict()` → `.model_dump()`
- `@validator` → `@field_validator`

### 2.2 field_validator 的 mode="before" 和 "after"？
- "before" 在类型转换前执行，拿到原始输入
- "after" 在转换后执行，拿到目标类型

### 2.3 computed_field 的作用？
- 定义派生字段，自动包含在序列化结果中
- 类似 @property 但参与 JSON 序列化

## 三、OpenAI SDK

### 3.1 Tool Calling 的完整流程？
1. 定义工具 schema（JSON Schema）
2. 调用 LLM，传入 tools 参数
3. LLM 返回 tool_calls
4. 解析 tool_calls，执行工具
5. 将工具结果作为 tool message 发回
6. 再次调用 LLM，整合结果

### 3.2 流式输出的实现方式？
- `stream=True`：原始 SSE 事件
- `.stream()`：类型化事件（推荐）

### 3.3 结构化输出怎么实现？
- `response_format` + Pydantic 模型
- `client.beta.chat.completions.parse()` 自动校验

## 四、LangChain / LangGraph

### 4.1 LangChain 和 LangGraph 的关系？
- LangChain 提供基础组件（@tool、LCEL、模型集成）
- LangGraph 提供图编排能力
- 生产推荐 LangGraph

### 4.2 LCEL 是什么？
- LangChain Expression Language
- 用 `|` 管道符组合 Runnables
- 提供声明式的流式、异步、批处理能力

### 4.3 LangGraph 的 Reducer 是什么？
- 定义状态更新如何合并
- `Annotated[list, operator.add]` = 追加到列表
- 默认 = 替换

### 4.4 Checkpointing 有什么用？
- 保存图状态快照
- 支持对话持久化、时间旅行调试、中断后恢复

### 4.5 ReAct 循环的工作原理？
1. LLM 思考（Reasoning）
2. LLM 决定调用工具（Action）
3. 执行工具，获取结果（Observation）
4. 将结果反馈给 LLM
5. 重复 1-4 直到 LLM 给出最终答案

## 五、FastAPI

### 5.1 FastAPI 的优势？
- 基于 Pydantic 自动校验
- 基于类型提示自动生成文档
- 原生异步支持
- 依赖注入系统

### 5.2 依赖注入怎么用？
- `Depends()` 声明依赖
- 支持依赖链、yield 资源管理
- 测试时用 `app.dependency_overrides` 替换

### 5.3 流式响应怎么实现？
- `StreamingResponse` + 生成器
- `media_type="text/event-stream"`（SSE）

## 六、asyncio

### 6.1 Task 和协程的区别？
- 协程惰性，不主动执行
- Task 创建后立即调度执行

### 6.2 gather 和 wait 的区别？
- gather 返回结果列表（按传入顺序）
- wait 返回 (done, pending) 集合，支持 FIRST_COMPLETED 和超时

### 6.3 为什么需要 Semaphore？
- 控制并发数，防止服务器过载或连接池耗尽

## 七、测试

### 7.1 fixture 的 scope 有哪些？
- function（默认）、class、module、session

### 7.2 conftest.py 的作用？
- 放置共享 fixture 和 hooks
- 自动被同级及子目录的测试发现

### 7.3 怎么测试 Agent？
- 三层：单元测试工具、Mock LLM 测试决策、可选端到端测试
