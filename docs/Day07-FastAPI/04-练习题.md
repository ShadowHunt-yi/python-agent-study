# Day 07 练习题

## 练习 1：Agent API

```python
# 1.1 实现一个完整的 Agent API
# POST /agent/chat        — 同步调用
# POST /agent/chat/stream — 流式调用
# GET  /agent/threads     — 获取对话列表
# GET  /agent/threads/{id} — 获取对话详情
```

## 练习 2：认证中间件

```python
# 2.1 实现 JWT 认证中间件
# - 白名单路由（/health, /docs）跳过认证
# - 其他路由需要 Bearer token
# - 验证 token 并注入 user 信息
```

## 练习 3：依赖注入

```python
# 3.1 实现数据库依赖
# - get_db: yield 数据库连接
# - get_current_user: 依赖 get_db，验证用户
# - 所有 /api/* 路由都需要认证
```
