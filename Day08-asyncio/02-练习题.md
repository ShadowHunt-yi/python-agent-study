# Day 08 练习题

## 练习 1：并发控制

```python
# 1.1 实现一个并发下载器
# - 最多同时下载 5 个文件
# - 支持超时（每个文件最多 10s）
# - 返回成功/失败统计

async def download_file(url: str, timeout: float = 10) -> bytes:
    """下载单个文件（模拟）"""
    await asyncio.sleep(random.uniform(0.5, 3))
    if random.random() < 0.1:
        raise ConnectionError(f"下载失败: {url}")
    return b"file content"

async def batch_download(urls: list[str], max_concurrent: int = 5) -> dict:
    pass
```

## 练习 2：生产者-消费者

```python
# 2.1 实现一个任务处理系统
# - 3 个生产者生成任务
# - 5 个消费者处理任务
# - 每个任务处理时间随机 0.1-1s
# - 统计总处理时间和吞吐量
```

## 练习 3：异步 HTTP

```python
# 3.1 用 httpx 实现并发 API 调用
# 同时调用 3 个 API，汇总结果
async def aggregate_data() -> dict:
    async with httpx.AsyncClient() as client:
        # 并发调用天气、新闻、股票 API
        pass
```
