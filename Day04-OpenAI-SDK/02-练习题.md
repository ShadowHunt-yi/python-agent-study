# Day 04 练习题

## 练习 1：基础调用

```python
# 1.1 写一个函数，接收用户消息，返回 LLM 回复
async def chat(message: str, model: str = "gpt-4o") -> str:
    pass

# 1.2 写一个批量调用函数，同时发送 5 个请求
async def batch_chat(messages: list[str]) -> list[str]:
    pass
```

## 练习 2：Tool Calling

```python
# 2.1 定义两个工具：calculate 和 search
# calculate: 接收 expression 字符串，返回计算结果
# search: 接收 query 字符串，返回搜索结果

# 2.2 实现完整的 ReAct 循环
async def react_agent(query: str, tools: list, max_steps: int = 5) -> str:
    # 循环：LLM 思考 → 调用工具 → 获取结果 → 继续思考
    # 直到 LLM 给出最终答案或达到最大步数
    pass
```

## 练习 3：结构化输出

```python
# 3.1 从非结构化文本中提取结构化信息
class MovieReview(BaseModel):
    title: str
    rating: float      # 1-10
    pros: list[str]
    cons: list[str]
    recommend: bool

async def extract_review(text: str) -> MovieReview:
    pass
```
