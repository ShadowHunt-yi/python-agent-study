# Day 01 练习题

## 练习 1：变量与类型

```python
# 1.1 以下哪些是合法的变量名？
my_var = 1
_my_var = 2
my-var = 3      # ?
2nd_name = 4    # ?
class = 5       # ?
MY_CONST = 6
```

```python
# 1.2 以下代码输出什么？
x = 10
if True:
    x = 20
    y = 30
print(x)
print(y)
```

```python
# 1.3 类型判断
print(type(True))
print(type(None))
print(type({}))
print(type(()))
print(type((1,)))
```

## 练习 2：控制流

```python
# 2.1 用 match 实现一个简单的计算器
def calc(a: float, op: str, b: float) -> float:
    # 实现 +, -, *, / 四则运算
    # 除以 0 时返回 float('inf')
    # 未知运算符时抛出 ValueError
    pass

# 测试
assert calc(10, "+", 5) == 15
assert calc(10, "-", 5) == 5
assert calc(10, "*", 5) == 50
assert calc(10, "/", 5) == 2.0
assert calc(10, "/", 0) == float('inf')
```

```python
# 2.2 找出列表中的所有质数
def find_primes(n: int) -> list[int]:
    # 返回 2 到 n（含）之间的所有质数
    pass

assert find_primes(20) == [2, 3, 5, 7, 11, 13, 17, 19]
assert find_primes(2) == [2]
assert find_primes(1) == []
```

## 练习 3：函数

```python
# 3.1 实现一个函数，接受任意数量的数字，返回统计信息
def stats(*numbers: float) -> dict:
    # 返回 {"count": 数量, "sum": 总和, "avg": 平均值, "min": 最小, "max": 最大}
    pass

result = stats(1, 2, 3, 4, 5)
assert result == {"count": 5, "sum": 15, "avg": 3.0, "min": 1, "max": 5}
```

```python
# 3.2 实现一个带缓存的斐波那契函数
def fibonacci(n: int) -> int:
    # 不使用 functools.lru_cache，手动实现缓存
    pass

assert fibonacci(0) == 0
assert fibonacci(1) == 1
assert fibonacci(10) == 55
assert fibonacci(50) == 12586269025
```

## 练习 4：数据结构

```python
# 4.1 实现单词频率统计
def word_count(text: str) -> dict[str, int]:
    # 将文本按空格分割，统计每个单词出现的次数（不区分大小写）
    pass

result = word_count("Hello hello World world WORLD")
assert result == {"hello": 2, "world": 3}
```

```python
# 4.2 实现两个列表的交集、并集、差集
def set_operations(a: list, b: list) -> dict:
    # 返回 {"intersection": 交集, "union": 并集, "difference": a-b}
    # 结果去重并排序
    pass

result = set_operations([1, 2, 3, 4], [3, 4, 5, 6])
assert result == {
    "intersection": [3, 4],
    "union": [1, 2, 3, 4, 5, 6],
    "difference": [1, 2],
}
```

## 练习 5：推导式

```python
# 5.1 用一行推导式实现
# 生成 1-100 中能被 3 整除但不能被 5 整除的数的平方
result = ...  # 你的代码
assert result[:3] == [9, 36, 81]
```

```python
# 5.2 将嵌套字典扁平化
data = {
    "user": {
        "name": "Alice",
        "address": {
            "city": "NYC",
            "zip": "10001"
        }
    },
    "active": True
}

def flatten(d: dict, prefix: str = "") -> dict:
    # {"user.name": "Alice", "user.address.city": "NYC", ...}
    pass

result = flatten(data)
assert result == {
    "user.name": "Alice",
    "user.address.city": "NYC",
    "user.address.zip": "10001",
    "active": True,
}
```

## 练习 6：错误处理

```python
# 6.1 安全的类型转换
def safe_int(value, default: int = 0) -> int:
    # 尝试将 value 转为 int
    # 失败时返回 default
    # None 时也返回 default
    pass

assert safe_int("42") == 42
assert safe_int("abc", -1) == -1
assert safe_int(None) == 0
assert safe_int(3.7) == 3
assert safe_int(True) == 1
```

```python
# 6.2 自定义异常 + 链式调用
class ConfigError(Exception):
    pass

class Config:
    def __init__(self):
        self._data = {}
    
    def set(self, key: str, value):
        # 如果 key 不是字符串，抛出 ConfigError
        # 如果 value 是 None，抛出 ConfigError
        pass
    
    def get(self, key: str, default=None):
        # 如果 key 不存在，返回 default
        # 如果 key 不是字符串，抛出 ConfigError
        pass

# 测试
c = Config()
c.set("name", "Alice")
assert c.get("name") == "Alice"
assert c.get("missing", "default") == "default"
```

## 答案提示

所有练习的答案都在日常开发中很常见。重点练习：
1. 推导式的 if/else 顺序
2. `*args`/`**kwargs` 的使用
3. 异常处理的 EAFP 风格
4. dict 的 `.get()` 方法
5. 负索引和切片

如果卡住了，回去复习对应的章节。
