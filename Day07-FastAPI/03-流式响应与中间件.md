# 03 — 流式响应与中间件

## 一、StreamingResponse —— SSE

### SSE（Server-Sent Events）协议格式

```
data: 第一条消息\n\n       ← 双换行是消息分隔符
data: 第二条消息\n\n
event: custom\ndata: 带事件类型\n\n  ← event 字段，前端用 addEventListener 监听
id: 1\ndata: 带ID的消息\n\n          ← id 字段，断线重连时用 Last-Event-ID
retry: 5000\ndata: 重连间隔\n\n      ← retry 字段，告诉客户端重连等待时间
```

- JS 端：`const es = new EventSource("/stream"); es.onmessage = (e) => console.log(e.data)`
- JS 端监听自定义事件：`es.addEventListener("custom", (e) => console.log(e.data))`

```python
from fastapi import FastAPI
from fastapi.responses import StreamingResponse
import asyncio

app = FastAPI()

async def event_generator():
    for i in range(10):
        yield f"data: 消息 {i}\n\n"  # 注意双换行
        await asyncio.sleep(0.5)

@app.get("/stream")
async def stream():
    return StreamingResponse(
        event_generator(),
        media_type="text/event-stream",
        headers={"Cache-Control": "no-cache"},
    )
```

### Agent 流式输出

```python
from fastapi.responses import StreamingResponse
import json

async def agent_stream(query: str):
    """模拟 Agent 流式输出"""
    # 思考阶段
    yield f"data: {json.dumps({'type': 'thinking', 'content': '分析问题...'})}\n\n"
    await asyncio.sleep(0.5)

    # 工具调用
    yield f"data: {json.dumps({'type': 'tool_call', 'name': 'search', 'args': {'q': query}})}\n\n"
    await asyncio.sleep(0.5)

    # 回复
    for token in "这是 Agent 的回复".split():
        yield f"data: {json.dumps({'type': 'token', 'content': token})}\n\n"
        await asyncio.sleep(0.1)

    yield f"data: {json.dumps({'type': 'done'})}\n\n"

@app.post("/agent/stream")
async def agent_chat_stream(req: ChatRequest):
    return StreamingResponse(
        agent_stream(req.message),
        media_type="text/event-stream",
    )
```

## 二、中间件

```python
from fastapi import FastAPI, Request
from fastapi.middleware.cors import CORSMiddleware
import time

app = FastAPI()

# CORS 中间件
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],           # 生产环境限制具体域名
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# 自定义中间件
@app.middleware("http")
async def add_timing(request: Request, call_next):
    start = time.perf_counter()
    response = await call_next(request)
    elapsed = time.perf_counter() - start
    response.headers["X-Process-Time"] = f"{elapsed:.4f}"
    return response
```

### 认证中间件

```python
from fastapi import Request, HTTPException

@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # 跳过公开路由
    if request.url.path in ["/", "/health", "/docs"]:
        return await call_next(request)

    auth = request.headers.get("Authorization")
    if not auth or not auth.startswith("Bearer "):
        raise HTTPException(status_code=401, detail="未认证")

    return await call_next(request)
```

## 三、后台任务

```python
from fastapi import BackgroundTasks

def send_notification(user_id: str, message: str):
    # 耗时操作
    time.sleep(2)
    print(f"通知 {user_id}: {message}")

@app.post("/orders")
async def create_order(
    order: OrderRequest,
    background_tasks: BackgroundTasks,
):
    # 创建订单...
    background_tasks.add_task(send_notification, order.user_id, "订单已创建")
    return {"order_id": "123"}
```

## 四、静态文件和模板

```python
from fastapi.staticfiles import StaticFiles
from fastapi.templating import Jinja2Templates
from fastapi import Request

app.mount("/static", StaticFiles(directory="static"), name="static")
templates = Jinja2Templates(directory="templates")

@app.get("/page/{page_name}")
async def page(request: Request, page_name: str):
    return templates.TemplateResponse(f"{page_name}.html", {"request": request})
```

## 五、面试高频问题

**Q: 怎么实现 Agent 流式输出？**
A: 用 `StreamingResponse` + 生成器，`media_type="text/event-stream"`。前端用 EventSource 接收。

**Q: 中间件的执行顺序？**
A: 先添加的中间件最后执行（洋葱模型）。请求从外到内，响应从内到外。

**Q: BackgroundTasks 的限制？**
A: 只适合轻量级任务（发邮件、写日志）。重量级任务用 Celery/RQ。
