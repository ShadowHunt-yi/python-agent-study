# Day 03 练习题

## 练习 1：API 请求/响应模型

```python
# 1.1 定义一个聊天 API 的请求/响应模型
class ChatRequest(BaseModel):
    # message: 用户消息（1-10000字符）
    # temperature: 温度（0-2，默认0.7）
    # max_tokens: 最大 token 数（1-4096，默认1024）
    # tools: 可选的工具列表
    pass

class ChatResponse(BaseModel):
    # reply: 模型回复
    # tokens_used: 使用的 token 数
    # model: 使用的模型名
    # finish_reason: 结束原因（stop/length/tool_calls）
    pass

# 测试
req = ChatRequest(message="Hello")
assert req.temperature == 0.7
assert req.max_tokens == 1024

resp = ChatResponse(reply="Hi!", tokens_used=5, model="gpt-4o", finish_reason="stop")
assert resp.tokens_used > 0
```

## 练习 2：嵌套模型 + 验证器

```python
# 2.1 定义一个 Agent 工具调用模型
class ToolCall(BaseModel):
    # name: 工具名称（只允许字母、数字、下划线）
    # arguments: 参数字典（JSON 字符串，需要在验证时解析为 dict）
    pass

class AgentDecision(BaseModel):
    # thought: 模型的思考过程
    # tool_calls: 工具调用列表（可空）
    # final_answer: 最终答案（可空）
    # tool_calls 和 final_answer 至少有一个
    pass

# 测试
decision = AgentDecision(
    thought="I need to search for info",
    tool_calls=[ToolCall(name="search", arguments='{"query": "Python"}')],
)
assert len(decision.tool_calls) == 1
```

## 练习 3：TypeAdapter

```python
# 3.1 验证配置数据
from pydantic import TypeAdapter

# 验证一个环境变量格式的 dict
# key 必须大写，value 必须是字符串
env_ta = TypeAdapter(dict[str, str])

# 验证一个 API key 列表
# 每个 key 格式: "sk-" 开头，长度 >= 20
api_keys_ta = TypeAdapter(list[str])

# 测试
env_ta.validate_python({"DB_HOST": "localhost", "DB_PORT": "5432"})
```
